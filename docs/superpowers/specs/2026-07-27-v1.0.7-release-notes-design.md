# v1.0.7 Version and Release Notes

## Version

Change the user-facing `contentScriptVersion` in `extension/content.js` from
`v1.0.6` to `v1.0.7`.

Keep `extension/manifest.json` at `0.1.2`. The manifest value is Chrome's
unpacked package version, while `contentScriptVersion` is the release label
rendered beside the pink 420 control and transported with `content.js`.

Update the existing version regression test to expect `v1.0.7`.

## Release-Note Scope

Propose a normal incremental release summary containing meaningful,
player-visible changes since v1.0.6. Do not repeat the complete extension
feature catalog and do not use technical commit summaries.

Keep the feature summary below 200 words, excluding any installation or update
instructions that may later accompany it. Group the notes under Terraforming
Mars.

Do not use the headings or lead-ins `Cleaner card controls:` or
`Stable queued play:`.

## Proposed Notes

### Terraforming Mars

- **Remembered quick actions:** Played-card choices can now be learned from
  either Actions panel, target controls, and numbered radio-option execution.
  Learned choices can be reused immediately during an unfinished action or
  placed behind that action in the queue.
- Long quick-action buttons wrap within their card instead of stretching the
  layout.
- Automatic and manual queue execution preserves what you were viewing while
  the top Actions panel rerenders. It leaves scrolling alone whenever that
  panel is visible.
- **Actions anywhere:** A fully interactive copy of the Actions panel appears
  below the extension controls. Pass defaults are synchronized across both
  panels.
- **Turn visibility:** The game background pulses pink when you can pass and
  yellow when another response is required.
- **Navigation keys:** `Q` centers Actions, `W` jumps to Played Cards, and `E`
  jumps to the bottom.

## Verification

Run the content-script syntax check, version-focused test, complete extension
test suite, and `git diff --check`. Confirm the manifest remains unchanged and
the final proposed feature summary remains below 200 words.
