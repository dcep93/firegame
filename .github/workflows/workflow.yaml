on:
  push:
    branches:
      - master
jobs:
  workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls
      - run: echo hi
      - run: echo $(ls) wut
      - run: bash .github/workflows/build_react.sh
      - run: bash .github/workflows/deploy_to_app_engine.sh
